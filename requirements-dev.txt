# AI Paimon — Development / CI dependencies
#
# Deliberately EXCLUDES torch / soundfile (the heavy VITS runtime): CI runs the
# bridge contract tests and lint offline, with no model weights and no network.

# Bridge runtime (needed to import clawbot_bridge and run its tests)
fastapi>=0.100
uvicorn>=0.23
python-dotenv>=1.0
websockets>=12.0
cryptography>=41.0
pydantic>=2.0

# Test + lint tooling
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.6
